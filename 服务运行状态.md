# 服务运行状态

最后更新: 2025-12-30

## ✅ 已启动的服务

1. **移动端前端** (voice_community_fe)
   - 端口: 5173
   - 状态: ✓ 运行中
   - 访问: http://localhost:5173

2. **主后端服务** (voice_community_backend)
   - 端口: 8081
   - 状态: ✓ 运行中
   - 访问: http://localhost:8081
   - API文档: 参考项目README

3. **后台管理前端** (voice_community_admin_fe)
   - 端口: 3000
   - 状态: ✓ 运行中
   - 访问: http://localhost:3000

4. **Web门户前端** (voice_community_web)
   - 端口: 5174
   - 状态: ✓ 运行中
   - 访问: http://localhost:5174

## ⚠️ 需要处理的服务

**后台管理后端** (voice_community_admin_backend)
- 端口: 8082
- 状态: ✗ 未运行
- 问题: Maven编译失败（Java编译器版本问题）
- 解决方案: 
  1. 在IDE中直接运行（推荐）
  2. 或确保使用Java 21并设置JAVA_HOME

## 所有服务访问地址

- 📱 移动端前端: http://localhost:5173
- 🖥️  主后端API: http://localhost:8081
- ⚙️  后台管理前端: http://localhost:3000
- 🔧 后台管理后端API: http://localhost:8082 (需要先编译运行)
- 🌐 Web门户前端: http://localhost:5174

## 快速启动命令

```bash
# 主后端（如果有JAR文件）
cd deploy && ./start-backend-local.sh

# 移动端前端
cd deploy && ./start-frontend-local.sh

# 后台管理前端
cd deploy && ./start-admin-frontend-local.sh

# Web门户前端
cd deploy && ./start-web-portal-local.sh

# 后台管理后端（需要先编译）
cd deploy && ./start-admin-backend-local.sh
```

## 检查服务状态

```bash
# 检查端口占用
lsof -i :8081 -i :8082 -i :5173 -i :3000 -i :5174

# 检查Java进程
jps -l | grep voice

# 检查Node进程
ps aux | grep vite
```
